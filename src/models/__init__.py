"""
src.models
==========
Model families for CxG, CxA, CxT.

Sub-packages:
  sequence/   — sequence type classifier (Phase 2)
  cxg/        — contextual finishing value models (Phases 4–5)
  cxa/        — contextual chance creation models (Phase 6)
  cxt/        — contextual possession-threat models (Phase 7)
  statistical/ — GLM and GAM implementations (Phases 5–7)
  trees/       — XGBoost / LightGBM / CatBoost wrappers (Phases 5–7)
  neural/      — FFNN, GNN, transformer models (Phases 5–7)
"""
